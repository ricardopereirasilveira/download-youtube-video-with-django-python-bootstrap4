from pytube import YouTube


def videoDownload(url):
    # yt = YouTube(url)
    # # video = yt.streams.order_by('resolution')[-1].download()
    # audio = yt.streams.filter(only_audio=True)[-1].download()
    # title = yt.title
    # urlVideo = 'https://www.youtube.com/watch?v=' + yt.video_id
    # views = yt.views
    # thumbnail = yt.thumbnail_url
    # author = yt.author
    pass